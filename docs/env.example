## BabyHandoff environment (example)
## Create a `.env` file in the repo root and copy these keys.
##
## IMPORTANT:
## - Do NOT commit `.env` (it's gitignored).
## - If your MongoDB password has special characters, URL-encode it.
##   Example: `@` -> `%40`, `:` -> `%3A`, `/` -> `%2F`, `?` -> `%3F`, `#` -> `%23`
##   Quick helper:
##     python -c "import urllib.parse; print(urllib.parse.quote_plus('YOUR_PASSWORD'))"

# --- MongoDB Atlas (required) ---
# Get this from Atlas → Connect → Drivers.
MONGODB_URI="mongodb+srv://<username>:<password>@<cluster-host>/?retryWrites=true&w=majority"

# Optional: database name inside the cluster (default: babyhandoff)
MONGODB_DB="babyhandoff"

# --- App ---
APP_ENV="dev"
APP_BASE_URL="http://127.0.0.1:8000"

# Demo mode toggles some UI + council behavior (MongoDB is still required).
DEMO_MODE=true

# Council mode: "heuristic" (no external LLM calls) or "openai"
COUNCIL_MODE="heuristic"

# Optional: OpenAI (only used when COUNCIL_MODE=openai)
OPENAI_API_KEY=""
OPENAI_MODEL="gpt-5.2"

