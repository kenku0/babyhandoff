# MongoDB Atlas connection string, e.g.:
# mongodb+srv://<user>:<pass>@cluster0.xxxxx.mongodb.net/babyhandoff?retryWrites=true&w=majority
MONGODB_URI=
MONGODB_DB=babyhandoff

# Optional (not required for DB driver connectivity; used only if you later call Atlas Admin APIs)
ATLAS_PROJECT_ID=

# App
APP_ENV=dev
APP_BASE_URL=http://127.0.0.1:8000

# Orchestration
AGENT_TIMEOUT_SECONDS=25
AGENT_MAX_RETRIES=2
CONTEXT_PACK_TOKEN_BUDGET=4000

# Demo
DEMO_MODE=true

# Council / LLM (optional)
COUNCIL_MODE=heuristic  # heuristic | openai
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.2
